name: Build Registry

on:
  push:
    branches: [main]
    paths:
      - 'patches/**'
      - 'scripts/build_registry.py'
  workflow_dispatch:

jobs:
  build-registry:
    runs-on: ubuntu-latest
    
    permissions:
      contents: write
    
    steps:
      - name: Checkout with submodules
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}
          submodules: recursive  # ← FUNDAMENTAL: busca todos os submodules
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pyyaml
      
      - name: Configure Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
      
      - name: Debug - List patches
        run: |
          echo "📁 Patches disponíveis:"
          ls -la patches/
          echo ""
          echo "📝 Verificando manifests:"
          find patches -name "manifest.yaml" -type f
      
      - name: Build registry (ZIPs + patches.json)
        env:
          AETHERCORE_REPO_USER: AsafViana
          AETHERCORE_REPO_NAME: aethercore-patches
          AETHERCORE_REPO_BRANCH: main
        run: |
          python scripts/build_registry.py
      
      - name: Check for changes
        id: check_changes
        run: |
          git add dist generated
          if git diff --cached --quiet; then
            echo "has_changes=false" >> $GITHUB_OUTPUT
            echo "✅ Nenhuma alteração detectada"
          else
            echo "has_changes=true" >> $GITHUB_OUTPUT
            echo "🔄 Mudanças detectadas em dist/ e generated/"
          fi
      
      - name: Commit and push changes
        if: steps.check_changes.outputs.has_changes == 'true'
        run: |
          git commit -m "chore: update dist and patches.json [skip ci]"
          git push
      
      - name: Summary
        if: steps.check_changes.outputs.has_changes == 'true'
        run: |
          echo "### ✅ Registry Atualizado" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**ZIPs gerados:**" >> $GITHUB_STEP_SUMMARY
          if ls dist/*.zip 1> /dev/null 2>&1; then
            ls -lh dist/*.zip | awk '{print "- " $9 " (" $5 ")"}'  >> $GITHUB_STEP_SUMMARY
          fi
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**patches.json:** generated/patches.json" >> $GITHUB_STEP_SUMMARY
